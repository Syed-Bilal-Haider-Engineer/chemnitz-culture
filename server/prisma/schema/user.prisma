model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  lat       Float
  lng       Float
  role      Role
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum Role {
  USER
  ADMIN
}
