// import AuthForm from '@/auth/AuthForm';
// import { register } from '@/lib/api';

// export default function RegisterPage() {
//   return <AuthForm isLogin={false} onSubmit={(email, password) => register({ email, password })} />;
// }
